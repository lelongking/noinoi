template(name="iGrid")
  .igrid(class="#{options.wrapperClasses}")
    if classicalHeader
      .igrid-header(class="#{classicalHeader.class}")
        +Template.dynamic(template=itemTemplate data=classicalHeader.columns)
    each dataSource
      +Template.dynamic(template=itemTemplate data=this)

template(name="testDyn")
  | #{name}, #{price}
  a.btn.btn-default Cloud